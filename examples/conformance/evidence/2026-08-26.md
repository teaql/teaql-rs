# Rust minimum runtime conformance evidence

Date: 2026-08-26

Model evaluation: 0 errors, 0 warnings, 0 suggestions, 8 solids.

```text
PASS ensure_schema (explicit SQLite DDL from Runtime Module)
PASS Checker (canonical title key, rejected before persistence)
PASS Create (id=2, version=1)
PASS Q API (typed SmartList<WorkItem>)
PASS E API (loaded, null fallback, and not-loaded are distinct)
PASS Update (version 1 -> 2)
PASS Delete (default Q excludes deleted rows)
PASS Rust minimum runtime conformance: 7/7
```

The root seed owns ID 1, so the first business entity receives ID 2. The run
also found and corrected ledger-backed Save returning version 0 after a create;
the returned typed entity now contains the authoritative runtime-managed version.

Runtime regression: 129 passed, 0 failed, plus one ignored documentation test.
